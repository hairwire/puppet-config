udp-port = <%= port %>   # the external port to listen on (configure your firewall)
mtu = <%= mtu %>         # minimum MTU of all outgoing interfaces on all hosts
ifname = <%= ifname %>    # the local network device name

